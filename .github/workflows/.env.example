# Переменные окружения для Frontend (React + Vite)
# Префикс VITE_ обязателен, чтобы Vite "увидел" переменную

# URL бэкенд API, к которому будет обращаться фронтенд
VITE_API_URL=http://localhost:8000